# Describes the action and it's parameters
name: "az"
description: "Executes Azure CLI command"
enabled: true
parameters:
  Command:
    type: "string"
    description: "Command to pass to azure cli"
    required: true
