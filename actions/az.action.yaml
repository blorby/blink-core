# Describes the action and it's parameters
name: "az"
description: "Executes Azure CLI command"
enabled: true
parameters:
  Command:
    type: "code:bash"
    description: "az command or a script containing az command"
    required: true
connection_types:
  azure:
    reference: azure
