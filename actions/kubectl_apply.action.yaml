name: "kubectl_apply"
collection_name: "k8s"
description: "Executes Kubernetes CLI (kubectl) apply command"
enabled: true
parameters:
  file:
    type: "code:yaml"
    description: "The yaml content to apply"
    required: true
