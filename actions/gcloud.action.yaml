# Describes the action and it's parameters
name: "gcloud"
description: "Executes Google Cloud (GCP) CLI command"
enabled: true
parameters:
  Command:
    type: "code:bash"
    description: "gcloud command or a script containing gcloud command"
    required: true
connection_types:
  gcp:
    reference: gcp
