# Describes the action and it's parameters
name: "aws"
description: "Executes aws cli command"
enabled: true
parameters:
  command:
    type: "string"
    description: "AWS CLI Command"
    required: true
  region:
    type: "string"
    description: "AWS Region for CLI Command"
    required: true
