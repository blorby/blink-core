# Describes the action and it's parameters
name: "http_delete"
description: "Executes delete request on provided url"
enabled: true
parameters:
  url:
    type: "string"
    description: "The url to send a delete request"
    required: true
