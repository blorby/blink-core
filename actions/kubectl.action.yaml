# Describes the action and it's parameters
name: "kubectl"
description: "Executes Kubernetes CLI (kubectl) command"
enabled: true
parameters:
  Command:
    type: "string"
    description: "Command to pass to kubectl"
    required: true
