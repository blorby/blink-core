# Describes the action and it's parameters
name: "kubectl"
description: "Executes Kubernetes CLI (kubectl) command"
enabled: true
parameters:
  Command:
    type: "code:bash"
    description: "kubectl command or a script containing kubectl command"
    required: true
