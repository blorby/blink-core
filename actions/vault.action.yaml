# Describes the action and it's parameters
name: "vault"
collection_name: "vault"
description: "Executes vault CLI command"
enabled: true
parameters:
  Command:
    type: "code:bash"
    description: "vault command or a script containing vault command"
    required: true
connection_types:
  vault:
    reference: vault
