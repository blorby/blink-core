# Describes the action and it's parameters
name: "bash"
collection_name: "bash"
description: "Executes user provided bash code"
enabled: true
parameters:
  code:
    type: "code:bash"
    description: "The actual code"
    required: true
