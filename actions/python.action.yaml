# Describes the action and it's parameters
name: "python"
collection_name: "python"
description: "Executes user provided python code"
enabled: true
parameters:
  code:
    type: "code:python"
    description: "The actual code"
    required: true
