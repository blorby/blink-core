name: "core"
description: "The core Blink plugin"
tags:
  - code
  - core
  - blink
  - internal

provider: "blink"
connection_types:
  core-mail:
    fields:
      test:
        field_type: "input"
        required: true
        placeholder: "Test"
        input_type: "text"
    reference: "aws"
